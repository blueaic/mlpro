## -------------------------------------------------------------------------------------------------
## -- Project : MLPro - The integrative middleware framework for standardized machine learning
## -- Workflow: pypi_deploy.yml
## -------------------------------------------------------------------------------------------------
## -- History :
## -- yyyy-mm-dd  Ver.      Auth.    Description
## -- 2021        1.0.0     MRD      Creation 
## -- 2024-10-09  1.1.0     DA       Update Python version to 3.10
## -- 2024-11-21  1.2.0     DA       Added the ability of manual execution
## -- 2025-04-27  1.3.0     DA       Update pypa/gh-action-pypi-publish@master to 
## --                                pypa/gh-action-pypi-publish@release/v1
## -- 2026-08-18  2.0.0     DA       Review and updates
## -------------------------------------------------------------------------------------------------

name: WF03 - Deployment PyPI

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    environment:
      name: deployment pypi
      url: https://pypi.org/p/mlpro

    permissions:
      contents: read
      id-token: write

    steps:

      - name: Checkout Codes
        uses: actions/checkout@v7

      - name: Set up Python 3.14
        uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Install pypa/build
        run: python -m pip install --upgrade build

      - name: Build a binary wheel and a source tarball
        run: python -m build --sdist --wheel --outdir dist/

      - name: Publish distribution to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
